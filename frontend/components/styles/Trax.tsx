import styled from 'styled-components';

const Trax = styled.h3`
  background: var(--black);
  color: white;
  display: inline-block;
  padding: 4px 5px;
  transform: skew(-3deg);
  margin: 0;
  font-size: 4rem;
`;

export default Trax;
